# Shared Types

This directory will eventually house generated schema definitions (Pydantic -> TypeScript) so
that the frontend and backend stay in sync. Stage 1 only stubs the folder in preparation for
those artifacts.
